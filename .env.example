# tt-sync server configuration
# Copy to .env and fill in values

# Required: API key for authentication
TT_SYNC_API_KEY=

# Data directory (where tasks.db is stored)
TT_DATA_DIR=./data

# HTTP port
PORT=8080
