# tt-sync server configuration
# Copy to .env and fill in values

# Required: API key for authentication
TT_SYNC_API_KEY=

# Host port for Docker (container always listens on 8080 internally)
TT_SYNC_PORT=8080

# Data directory (only relevant when running outside Docker)
# TT_DATA_DIR=./data
